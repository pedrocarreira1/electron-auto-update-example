{
  "name": "electron-auto-update-example",
  "version": "3.0.0",
  "description": "Just a auto update test",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/pedrocarreira1/electron-auto-update-example.git"
  },
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "Pedro Manuel",
  "license": "ISC",
  "devDependencies": {
    "electron": "^18.3.7",
    "electron-builder": "^21.2.0"
  },
  "dependencies": {
    "electron-log": "^3.0.9",
    "electron-updater": "^4.2.0"
  }
}
